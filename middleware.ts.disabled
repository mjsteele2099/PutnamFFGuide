// Temporarily disable authentication middleware so all routes are accessible.
// To re-enable authentication, restore the next-auth middleware export and update the matcher.
export const config = {
  matcher: [],
}

